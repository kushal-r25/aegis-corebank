spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/account_db?options=-c%20timezone%3DUTC
    username: bank
    password: bank
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
jwt:
  secret: test-secret-key-for-junit-only-min-32-bytes-long
